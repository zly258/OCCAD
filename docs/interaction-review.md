# CAD 交互检查记录

基线：cad / be7b7fb0cce246bf03e27fbfc7e32b4aa43737c9。
以下为本地工作区修复，尚未提交或推送。

## 已修改并验证

- 修复 WPF Panel 类型冲突和已删除状态栏控件的残留引用。
- ToolPanel 使用稳定的按钮/平面行容器，重复重建、切换语言和阶段不再重复挂载控件。
- 捕捉只解析目标点，不在悬停过程中替换绘图平面或强制解锁。工具采用明确选择的平面。
- 相同自定义工作平面的重复设置不再发送 Changed，消除无意义的刷新。
- 首点前允许切换工作平面，重新初始化工具缓存坐标系并保留参数；输入首点后或平面锁定时拒绝切换。
- 显式长度/角度约束优先于冲突捕捉；被约束改变的点不显示为精确对象捕捉。
- 无效组合精确输入在写入任何锁之前验证，避免部分更新。
- 工具阶段变化更新光标；离开视口清理临时输入；关闭窗口解绑静态语言事件。
- 补齐 39 项属性名称资源，以及多段线可闭合阶段的中文提示。

验证环境：本机 .NET 10，现有 Bridge SDK，D:\tools\occt-vc144-64。
运行了真实 OCCT 引擎的隐藏绘图表面，不是模拟捕捉结果。

- build.ps1：0 警告、0 错误。
- ToolPanel：切换语言、重复挂载/清空、阶段变化、取消清理通过。
- 16 种绘图工具首点前切换通过；矩形尺寸参数保留，提交几何位于 YZ 平面。
- 四条矩形边在 XY/YZ/XZ、跨越对边时的 60 组几何验证通过。
- 锁定/未锁定状态共 200 次实际端点捕捉：平面不改变，锁不丢失，不产生平面 Changed 循环。
- 实际端点捕捉与长度锁冲突时长度保持正确；无效组合输入不污染锁状态。

## 仍需继续验证和修复

- 可见 WPF 窗口中的完整鼠标操作：跨窗口拖动、捕获丢失、导航与工具切换、连续撤销/重做。
- 中点、中心、顶点、象限点在不同实体及复杂场景中的完整捕捉验收；大量实体性能。
- 属性值（枚举、布尔、颜色等）、动态命令输出、历史名称和错误消息的完整中文覆盖。
- 其他绘图工具在各平面的全部阶段、参数组合、返回上一步及锁定行为。
- ToolPanel 的 DPI、窗口缩放、焦点切换和 WinForms PropertyGrid 的实际显示/编辑体验。

入口保持为 git pull、.\build.ps1、.\run.ps1；需要发布时使用 .\publish.ps1。
运行库未配置时，run.ps1 支持 -OcctRoot 指定 OCCT SDK 路径。

## 扩展目标：核心 CAD 功能完整性

已补入 RotateTool、ScaleTool，沿用既有选择、预览、几何事务和历史体系。
菜单与命令行 RO/ROTATE、SC/SCALE 已接入。验证覆盖鼠标点输入、数值输入、
实际几何、预览隔离、取消、撤销/重做和命令行 Enter 确认选择。
所有活动工具均显示可关闭的 ToolPanel，工具标题新增中文资源。

对照 OCCTBIM-Source/release-1.0 的 src/tools，当前明确未完成的功能组：

| 功能组 | 参考实现 | 当前状态 |
| --- | --- | --- |
| 镜像 | edit/MirrorTool | 已实现保留原件/原位镜像；16 种实体通过原生创建与保存读取验证 |
| 矩形/环形/路径阵列 | edit/RectArrayTool、CircleArrayTool、PathArrayTool | 矩形、环形、路径阵列均已接入；路径阵列的折线工作流通过原生回归，完整可见界面验收待完成 |
| 距离与综合测量 | measure/MeasureDistanceTool、UnifiedMeasureTool | 两点距离已实现并验证；综合测量仍缺失 |
| 文字、表格、尺寸标注 | annotations 下工具 | BRep 文字以及长度、半径、直径、角度尺寸已实现；表格待实现 |
| 块插入 | 2d/InsertBlockTool | 缺失 |
| 拉伸、旋转体、扫掠、放样 | feature 下工具 | 缺失 |
| 并集、差集、交集 | feature/UnionFeatureTool、SubstractFeatureTool、IntersectFeatureTool | 缺失 |
| 倒角、加厚 | feature/ChamferFeatureTool、ThickenFeatureTool | 缺失 |
| 分割、提取边/面 | brep 下工具 | 缺失 |
| 椭圆弧、螺旋线、椭球等 | 2d/3d 对应工具 | 椭球、等半径三维螺旋线已接入并验证；椭圆弧及平面/圆锥 Spiral 仍缺失 |

该表是已确认缺口，不代表剩余完整验收只限于表中项目。
每项功能还需配套实体表示、编辑、文件保存/读取、历史记录、UI 和中文提示；
不能只增加菜单或调用 Bridge 方法就认定功能完成。

## 选择确认与距离测量

- 修改工具的面板“接受”、Enter 和右键统一确认已选对象；空选择不会意外提交。
- ToolPanel 显示可换行的完整操作提示；等待选择时显示工作平面未激活，禁用平面锁。
- 新增测量菜单与 DISTANCE/DIST/DI 命令。显示三维距离和 X/Y/Z 差值，支持实时预览、连续测量、返回、结束及取消。
- 原生验证覆盖 3/4/12 分量得到距离 13、重合点距离 0、中文结果显示和全部退出清理。
- 测量不生成文档实体，不改变文档修改状态或撤销历史。BRep 文字实体已经支持编辑、捕捉、夹点、变换、镜像和持久化；长度尺寸已在 XY/YZ/XZ 工作平面实现；半径尺寸支持圆和圆弧，直径尺寸支持圆；角度尺寸支持具有公共端点且不平行的两条直线，并支持任意三维平面。
## 命令反馈与属性布尔值

- 命令结果携带资源键与格式参数，补齐未知命令、不可用命令、无效坐标、步骤限制、闭合条件等 15 项中英文消息。
- 成功输入坐标/尺寸后保留当前工具的下一步提示，不再被输入回显覆盖。
- 命令行长消息提供完整悬浮提示；关闭窗口时解绑命令行的工具/动作事件。
- 属性布尔值中文显示为“是/否”，仍使用 bool 存储；兼容 True/False 输入。
- 原生回归验证再次通过；新增结构化消息验证覆盖未知命令参数、非法坐标、多段线闭合条件与取消。
- 枚举属性、原生异常详情、历史名称等仍需继续核对，不将上述修复视为全项目汉化完成。

## 矩形阵列

- 新增矩形阵列工具、修改菜单与 RECTARRAY/ARRAYRECT 命令。
- 行列数包括原件；间距沿启动工具时的工作平面 X/Y 轴，支持负间距。
- 支持预选择或进入工具后选择；参数面板、Enter 和右键可提交，取消不修改文档。
- 每个方向 1–100 格，至少 2 格；总副本最多 10000 个。超限在修改文档之前拒绝。
- 预览最多 200 个副本，提示显示总数/预览数；实际提交不会按预览数量截断。
- 副本使用现有实体类型并保留图层等属性，整个阵列作为一条新增实体历史记录。
- 原生验证：YZ 平面负间距定位、5 个副本数量/ID/图层、整批撤销重做、文件保存读取、零间距拒绝和取消。
- 原生验证：预览 200 个时实际提交全部 255 个副本；超限拒绝不污染文档或历史。
- 本轮构建 0 警告、0 错误。环形/路径阵列和其他核心功能缺口仍未完成。
## 环形阵列

- 新增修改菜单和 CIRCLEARRAY/ARRAYPOLAR 命令，数量包含原件。
- 使用当前工作平面法向作为阵列轴，指定中心后预览；支持 2–1000 个阵列位置、正负角度。
- 整圆按数量均分，不在 360 度重复原件；非整圆包含起始和终止角位置。
- 默认旋转副本；保持朝向模式要求明确的原件参考点，不能位于阵列轴上，所有选中对象作为一组平移。
- 预览最多 200 个副本，提交最多 10000 个副本；总数及预览数在提示中明确显示。
- 原生验证覆盖整圆分布、扇形终点、负角度、朝向保持、参考点拒绝/接受、返回上一步、撤销重做和保存读取。
- 构建 0 警告、0 错误。路径阵列、镜像、BRep 文字以及长度、半径、直径、角度尺寸已经完成原生回归；表格及更多实体特征仍未完成。
## 文档批量操作与历史可靠性

- 单个/批量新增统一处理：先创建全部原生图形，成功后登记实体与发布变更；失败清理已创建的显示对象。
- 单个/批量删除与清空模型统一处理：原生清理成功后再移除文档记录；失败尝试重建显示和恢复选择，恢复失败明确抛出聚合错误。
- 撤销/重做在条目执行成功后才移动历史记录；失败保持记录与状态编号，允许重试。
- 文档可编辑性检查排除不属于当前文档的实体。
- 已用真实 OCCT 微小退化长方体触发第二项创建失败：无实体残留、无原生对象残留、无成功变更通知、修改标志不变，首个实体可再次添加。
- 已验证撤销/重做失败后记录保留且可重试。既有原生绘图、阵列、保存读取回归全部通过，构建 0 警告、0 错误。
- 删除过程中引擎故障的恢复分支尚未完成实际故障注入验收；不能把普通删除/撤销通过等同于该分支已验证。

## 镜像

- 对照 OCCTBIM 的 MirrorTool，增加 MIRROR/MI、修改菜单、保留原件开关和中文操作提示。
- 工作平面内两点定义镜像线；实际反射平面包含该线及工作平面法向。拒绝重合点及平行法向的无效线。
- 保留原件时创建新 ID；原位镜像保持原实体及 ID，沿用几何历史记录。
- 16 种现有实体保持可编辑类型。长方体反射后调整底面参考与 Z 轴，以保持右手坐标系及正尺寸。
- 原生验证通过：复制/原位流程、无效轴不修改文档、撤销重做、16 种实体双次镜像、原生创建及保存读取。
- 长方体另按反射公式独立核对一次镜像的 8 个角点，避免仅用双次镜像掩盖错误。
- 本轮再次运行根目录 build.ps1：0 错误、0 警告；既有交互原生回归全部通过。
- 尚未完成可见 WPF 窗口的完整鼠标与布局验收；本记录不表示全部 CAD 功能或全部交互问题已经解决。
## 捕捉状态与高频刷新

- 默认端点/中点/中心/顶点/象限点模式不再构建只供最近点、交点、垂足和切点使用的精确曲线。
- 关闭 Enabled 立即清空当前候选与标记，不再等待下一次鼠标移动。
- 文档修改或删除候选实体时立即清空候选；全局变更同样失效，避免 Tab 循环使用旧几何。
- 空状态重复 Clear 不再发送 CurrentChanged，减少工具切换/鼠标离开期间的重复 UI 调度。
- 真实 OCCT 验证：禁用/恢复、端点编辑后重新捕捉、删除目标、100 次空清理均通过；已有原生回归全部通过。
- build.ps1：0 错误、0 警告。尚未完成大模型性能和全部捕捉类型的可见界面验收，不宣称所有卡顿已经解决。
## 圆心不在可见边线时漏捕捉

- 独立 OCCT 视口确认原实现失败：只有一个半径 100 的圆，鼠标位于圆心仍返回无捕捉。根因是先按边线矩形拾取过滤实体，圆心处没有圆周。
- 中心点独立建立屏幕位置缓存，按 X 坐标排序并二分筛选附近候选；不要求先命中可见边线。
- 缓存包含相机、投影、视口尺寸、工作平面策略；文档变化立即失效。隐藏/不可选择实体不进入缓存，透视相机背后的中心不进入缓存。
- 原生验证通过：圆心、平移、缩放、透视/FOV、窗口尺寸、中心坐标编辑、隐藏恢复、平面限制和投影策略。
- 401 个圆场景中，缓存建立后连续 200 次中心捕捉实测约 4 ms。该数据不代表完整 UI 帧率或任意复杂模型性能。
- 未完成全部类型、遮挡关系和大模型的最终界面验收；完整 CAD 目标仍在推进。
透视验证补充：独立平面场景直接切换投影曾出现 Eye 与 Center 重合，不能用该退化相机证明透视正确。
后续用显式非零眼睛距离验证，前方中心捕捉及后方中心排除均通过。
Bridge 在平面场景下的退化相机来源仍需后续核对；本轮未修改 Bridge，也未认定该问题已解决。
最终构建 0 错误、0 警告，全部原生回归通过；401 圆场景的最终 200 次查询为 3 ms。
## 属性枚举值汉化

- 属性网格接入材质、线型和显示模式的本地化转换；补齐 25 种材质、5 种线型、2 种显示模式的中英文资源。
- 中文下拉/输入转换回原始枚举值，保留规范英文输入；不修改实体枚举定义及文件格式。
- 16 种实体的只读类型值汉化，正多边形的存储名 Regular Polygon 映射到已有资源键；补齐方向类别。
- 通过实际属性描述器验证全部 32 个枚举值中文转换、原值回写、英文兼容输入与保留描述器的语言切换。
- 16 种实体类型的本地化及只读状态验证通过。构建 0 错误、0 警告；既有原生回归全部通过。
- 本轮未认定颜色对话框、历史名称、异常提示或全部 UI 汉化完成；可见 PropertyGrid 的完整编辑体验仍需验收。
## 撤销重做名称与语言切换

- 菜单及历史提示统一在显示时翻译内建操作，覆盖创建、复制、删除、隐藏、移动、旋转、缩放、夹点、阵列、镜像及图层操作。
- 属性编辑记录改为稳定的 Property + PropertyDescriptor.Name；不再把当时语言的显示标签写入历史。
- 图层表格编辑同样保存稳定名称。语言切换后旧记录可以按当前语言显示。
- 动态实体名、图层名作为原文参数保留；数量及显示模式独立翻译；未知操作保持原始文本。
- 验证通过：全部现有静态内建名称、动态数量/属性/显示模式、包含格式占位符的用户名称、真实新增/撤销记录及其语言切换。
- 构建 0 错误、0 警告；既有原生回归全部通过。旧进程中已存入中文的历史名称不做逆向猜测转换。
- 本轮没有完成全部异常消息汉化和可见属性网格操作验收；核心 CAD 功能缺口仍需继续实现。
## 路径阵列：原生路径采样已实现，功能仍在开发

- 对照 PathArrayTool，使用 Bridge 的原生曲线长度和按弧长求点/切线接口，不将曲线降级为折线。
- CadArrayPath 支持直线、多段线、多边形、正多边形、矩形、圆、圆弧、椭圆和插值样条，独立持有并释放建模会话。
- 有序线框按全路径累计长度，反向边同步修正切线方向；折点取出边切线。
- 开放路径在间距整除时包含终点，闭合路径排除重复接缝；无效间距、退化边及数量超限明确拒绝。
- 原生验证通过：长度 20 的折线间距 6 时第三个位置位于下一条边的 2 处；圆周长度、半周对点、闭合去重、9 种曲线采样、单位切线及释放检查。
- 目前只完成采样基础，尚未接入路径选择、源参考点、朝向选项、预览、中文 UI 和副本历史提交。不得将此条视为路径阵列功能完成。
## 路径阵列：工具、预览与提交已接入

- 新增 PATHARRAY/ARRAYPATH、修改菜单和中文工具面板。支持预选源对象或确认选择后，选独立路径、指定参考点、设置间距与沿切线转向。
- 原件保留；副本包括路径起点站位。参考点映射到每个站位，源对象组保持相对位置。
- 沿切线转向使用工具启动时工作平面 X 轴为前向，以最短旋转对齐三维切线；反向 180 度用工作平面法向定轴。关闭转向时保持原朝向。
- 不允许路径同时作为源对象；源对象或路径被编辑/删除后取消工具，避免提交过期快照。取消和返回路径选择释放原生采样资源。
- 预览最多 200 个、总副本最多 10000；超限在修改文档前拒绝。提交不按预览数量截断，沿用整批新增实体历史。
- 原生回归通过：别名、路径/参考点阶段、跨折点坐标及朝向、保持朝向、201 副本提交、超限拒绝、整批撤销重做、保存读取、返回与路径编辑清理。
- 构建 0 错误、0 警告。9 种路径类型的采样已验证，但本轮完整副本流程主要覆盖折线；可见窗口点击、多对象复杂空间路径、各类型边界情况仍待继续验收。

## 椭球实体基础（工具尚未接入）

- 新增原生 CadEllipsoidEntity，保存中心、右手坐标架及 X/Y/Z 三个半轴，不因旋转、镜像或保存读取丢失方向。
- 支持中心/六轴端点捕捉和夹点、平移、旋转、等比缩放、镜像、复制、几何恢复及 JSON 保存读取。
- 通过全实体原生创建、双次镜像和保存读取检查，当前覆盖实体数由 16 增至 17。
- 补齐椭球及三个半轴属性资源，构建 0 错误、0 警告。
- EllipsoidTool、菜单、命令和分阶段预览尚未接入，不能将椭球功能标为完成。


## 椭球工具已接入

- 新增 ELLIPSOID/ELL、三维菜单及四阶段中文提示：中心、X 半轴、Y 半轴、Z 半轴。
- X/Y 半轴在启动工作平面定义方向；确定 Y 后切换到由 Z/X 轴组成的垂直平面，避免 Z 半轴被二维平面投影成零。
- 支持逐级 Backspace，退回 Y 半轴时恢复原工作平面；取消清理预览和锁定状态。
- 原生验证覆盖精确 4/3/2 半轴、32π 体积、7 个捕捉点、7 个夹点、撤销重做、完整返回和取消。
- 17 种实体的原生创建、双次镜像及保存读取回归通过；构建 0 错误、0 警告。
- 椭圆弧、螺旋线和其他功能仍在缺口中；可见窗口鼠标体验尚待验收。


## 三维螺旋线

- 新增精确原生 Helix 实体及 HELIX/HEL、三维菜单和中文参数面板；中心与半径由鼠标指定，螺距和圈数在面板编辑。
- 支持任意轴、起始方向、正负螺距和小数圈数；属性、捕捉、夹点、变换、镜像、保存读取及路径阵列采样均已接入。
- 镜像同步改变右手坐标架和螺距符号，保持实际反射曲线；等比缩放同步缩放半径和螺距。
- 原生验证覆盖半径 4、螺距 -2、2.5 圈、精确终点/高度、撤销重做、返回和取消。18 种实体原生创建/镜像/保存及 10 种路径采样回归通过。
- Bridge 未提供精确修剪椭圆构造；未用样条冒充椭圆弧。参考项目的平面/圆锥 Spiral 模式也尚未实现。




